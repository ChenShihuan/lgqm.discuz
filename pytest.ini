[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: 单元测试（纯函数，无 I/O）
    integration: 集成测试（mock I/O）
    e2e: 端到端测试（真实文件系统/网络）
    slow: 耗时测试
